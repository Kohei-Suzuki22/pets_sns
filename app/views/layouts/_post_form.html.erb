<%= form_for(model) do |f| %>

  <%= render "layouts/error_messages",model: f.object %>

  <div class="form-group">
    <%= f.text_area :content, rows:4, class:"form-control", placeholder:"テキスト" %>  
  </div>
  
  <%= f.submit "投稿する", class:"btn btn-primary btn-block" %>

<% end %>